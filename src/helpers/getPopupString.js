const getPopupString = ({ name, population, averageAge }) =>
  `<p>Name: <span>${name}</span></p><p>Population: <span>${population}</span></p><p>Average age: <span>${averageAge}</span></p>`;

export default getPopupString;
